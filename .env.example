# ------------------------------------------------------------
# لا يحتاج المشروع أي متغيرات بيئة في وضعه الحالي (static + localStorage).
# هذه المتغيرات مطلوبة فقط عند تفعيل الـ API (راجع docs/api-example).
# أضفها من: Vercel → Project → Settings → Environment Variables
# ------------------------------------------------------------

# كلمة مرور لوحة التحكم في وضع الـ API (تُرسل كـ Bearer token)
ADMIN_TOKEN=

# تُضاف تلقائيًا عند ربط Upstash Redis من Vercel Marketplace
KV_REST_API_URL=
KV_REST_API_TOKEN=
